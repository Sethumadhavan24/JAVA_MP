# Database Connection Settings (JDBC)
spring.datasource.url=jdbc:mysql://localhost:3306/marketplace_db
spring.datasource.username=root
spring.datasource.password=Asss24805!

# Hibernate/JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Set backend port to 8081 so frontend can reach http://localhost:8081
server.port=8081
# Bind to all addresses (useful if testing from other devices); remove if you only want localhost
server.address=0.0.0.0

# Optional: reduce log verbosity or help debugging
logging.level.org.springframework=INFO